<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MySolarSystem.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MySolarSystem.Pages"
    FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:Home}" />-->
    <FlyoutItem Title="Home" Icon="icon_home.svg">
        <ShellContent Title="Home" Icon="icon_house.svg" Route="home"
                      ContentTemplate="{DataTemplate local:Home}"/>
        <ShellContent Title="Lesson" Icon="icon_books.svg" Route="lessons"
                      ContentTemplate="{DataTemplate local:Lessons}" />
        <ShellContent Title="Quiz" Icon="icon_exam.svg" Route="quiz"
                      ContentTemplate="{DataTemplate local:Quiz}" />
    </FlyoutItem>

</Shell>
